apiVersion: v1
kind: ConfigMap
metadata:
  name: label-studio-config
  labels:
    app: rtmlib-ml-backend
data:
  # ML Backend Configuration
  BACKEND_PORT: "9090"
  DEVICE: "cpu"  # Change to "cuda" for GPU nodes
  BACKEND_TYPE: "onnxruntime"
  MODE: "balanced"  # performance, lightweight, balanced
  CONFIDENCE_THRESHOLD: "0.3"
  DEBUG: "false"
  LOG_LEVEL: "INFO"
  url: "http://label-studio-service:8080"
---
apiVersion: v1
kind: Secret
metadata:
  name: label-studio-secret
  labels:
    app: rtmlib-ml-backend
type: Opaque
stringData:
  api-token: "your-label-studio-api-token"
  
